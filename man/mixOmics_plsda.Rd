% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixOmics_plsda.R
\name{mixOmics_plsda}
\alias{mixOmics_plsda}
\title{PLS-DA step for scLASER objects using mixOmics}
\usage{
mixOmics_plsda(
  object,
  response_var = "cell_type",
  use_nam = TRUE,
  multilevel_var = NULL,
  ncomp = 2,
  ...
)
}
\arguments{
\item{object}{A `scLASER` object.}

\item{response_var}{Character. Name of the column in `object@metadata`
to use as the class label (default `"cell_type"`).}

\item{use_nam}{Logical. If `TRUE` (default), use `object@nam_pcs`;
if `FALSE`, use `object@pcs` instead.}

\item{multilevel_var}{Optional character. Name of the metadata column
identifying the unit for repeated measures (e.g. `"sample_id"`).
If not `NULL`, multilevel PLS-DA is performed by applying
[mixOmics::withinVariation()] before `plsda()`.}

\item{ncomp}{Integer. Number of latent components to compute.
Passed to [mixOmics::plsda()].}

\item{...}{Additional arguments passed on to
[mixOmics::plsda()].}
}
\value{
A `mixOmics` `plsda` object.
}
\description{
Convenience wrapper around [mixOmics::plsda()] that pulls the
feature matrix from a `scLASER` object, constructs the class labels
from a metadata column, optionally performs multilevel decomposition
with [mixOmics::withinVariation()], and returns the fitted PLS-DA model.
}
